package main

// methods set: methods attached to a type
// a non-pointer receiver: works with pointers or non-pointers
// a pointer receiver: only works with values that are pointers

func main() {

}
